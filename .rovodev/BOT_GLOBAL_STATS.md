# 📊 ГЛОБАЛЬНА СТАТИСТИКА БОТІВ

## ✅ ЩО БУЛО РЕАЛІЗОВАНО

Додано систему глобальної статистики для відображення загальних показників кожного бота (від усіх користувачів).

---

## 🗄️ НОВА ТАБЛИЦЯ: `bot_global_stats`

### Структура:
```sql
CREATE TABLE bot_global_stats (
  id UUID PRIMARY KEY,
  bot_id UUID REFERENCES telegram_bots(id),
  
  -- Статистика
  total_users INTEGER DEFAULT 0,      -- Скільки користувачів використовує цього бота
  total_channels INTEGER DEFAULT 0,   -- Скільки каналів підключено
  total_posts INTEGER DEFAULT 0,      -- Скільки постів опубліковано
  
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ,
  
  UNIQUE(bot_id)
);
```

### Дані в таблиці:
```
bot_id: UUID бота
total_users: 15        ← 15 користувачів використовують цього бота
total_channels: 23     ← 23 канали підключено до цього бота
total_posts: 1542      ← 1542 пости опубліковано цим ботом
```

---

## 🔄 АВТОМАТИЧНЕ ОНОВЛЕННЯ

### 1️⃣ **Користувачі (total_users)**

#### ➕ **+1 користувач** коли:
```sql
-- Користувач додає ПЕРШИЙ канал з цим ботом
INSERT INTO bot_services (user_id: "user1", bot_id: "bot-uuid") 
→ total_users + 1

-- або
INSERT INTO ai_bot_services (user_id: "user1", bot_id: "bot-uuid")
→ total_users + 1
```

#### ➖ **-1 користувач** коли:
```sql
-- Користувач видаляє ОСТАННІЙ канал з цим ботом
DELETE FROM bot_services WHERE user_id = "user1" AND bot_id = "bot-uuid"
→ якщо у user1 немає більше каналів з bot-uuid → total_users - 1
```

**Приклад:**
```
Користувач "user1" має:
  - Канал #1 з bot-uuid
  - Канал #2 з bot-uuid

Видаляє канал #1 → total_users залишається (є ще канал #2)
Видаляє канал #2 → total_users - 1 (більше немає каналів)
```

---

### 2️⃣ **Канали (total_channels)**

#### ➕ **+1 канал** коли:
```sql
INSERT INTO bot_services → total_channels + 1
-- або
INSERT INTO ai_bot_services → total_channels + 1
```

#### ➖ **-1 канал** коли:
```sql
DELETE FROM bot_services → total_channels - 1
-- або
DELETE FROM ai_bot_services → total_channels - 1
```

---

### 3️⃣ **Пости (total_posts)**

#### ➕ **+1 пост** коли:
```sql
-- Плагіатор публікує пост
INSERT INTO posts_history (status: 'published') → total_posts + 1

-- AI-бот публікує пост
UPDATE ai_generated_posts SET status = 'published' → total_posts + 1
```

#### ➖ **-1 пост** коли:
```sql
-- Видалення опублікованого посту
DELETE FROM posts_history WHERE status = 'published' → total_posts - 1
DELETE FROM ai_generated_posts WHERE status = 'published' → total_posts - 1
```

**Важливо:** Видалення постів зі статусом `scheduled` або `failed` **НЕ впливає** на лічильник!

---

## 🎨 ВІДОБРАЖЕННЯ В UI

### Компонент: `BotSelector.tsx`

#### Було:
```tsx
<div className="grid grid-cols-2 gap-4">
  <div>Користувачів: {bot.users_count}</div>
  <div>Uptime: {uptime}%</div>
</div>
<div className="grid grid-cols-2 gap-4">
  <div>Постів: {bot.posts_count}</div>
  <div>Каналів: {bot.channels_count}</div>
</div>
```

#### Стало:
```tsx
{/* Глобальна статистика */}
<div className="bg-muted/30 rounded-lg p-3">
  <p className="text-xs">📊 Загальна статистика</p>
  <div className="grid grid-cols-3 gap-2">
    <div className="text-center">
      <p className="text-lg font-bold">{stats.total_users}</p>
      <p className="text-xs">Користувачів</p>
    </div>
    <div className="text-center">
      <p className="text-lg font-bold">{stats.total_channels}</p>
      <p className="text-xs">Каналів</p>
    </div>
    <div className="text-center">
      <p className="text-lg font-bold">{stats.total_posts}</p>
      <p className="text-xs">Постів</p>
    </div>
  </div>
</div>

<div className="grid grid-cols-2 gap-4">
  <div>Uptime: {uptime}%</div>
  <div>Навантаження: {uptime > 80 ? '🟢' : '🟡'} {uptime}%</div>
</div>
```

---

## 🔧 ТРИГЕРИ

### Створені тригери:

| Таблиця | Подія | Тригер | Дія |
|---------|-------|--------|-----|
| `bot_services` | INSERT | `trigger_bot_stats_on_bot_service_insert` | +1 канал, +1 користувач (якщо перший) |
| `bot_services` | DELETE | `trigger_bot_stats_on_bot_service_delete` | -1 канал, -1 користувач (якщо останній) |
| `ai_bot_services` | INSERT | `trigger_bot_stats_on_ai_bot_service_insert` | +1 канал, +1 користувач (якщо перший) |
| `ai_bot_services` | DELETE | `trigger_bot_stats_on_ai_bot_service_delete` | -1 канал, -1 користувач (якщо останній) |
| `posts_history` | INSERT | `trigger_increment_posts_on_publish` | +1 пост (якщо status='published') |
| `posts_history` | DELETE | `trigger_decrement_posts_on_delete` | -1 пост (якщо був published) |
| `ai_generated_posts` | UPDATE | `trigger_increment_ai_posts_on_publish` | +1 пост (при зміні на published) |
| `ai_generated_posts` | DELETE | `trigger_decrement_ai_posts_on_delete` | -1 пост (якщо був published) |

---

## 📊 ПРИКЛАДИ

### Приклад 1: Новий користувач додає канал
```
Початковий стан бота "MyBot":
  total_users: 10
  total_channels: 15
  total_posts: 500

Користувач "user1" додає канал #1:
→ total_users: 11  ✅ (+1, перший канал користувача)
→ total_channels: 16  ✅ (+1)
→ total_posts: 500

Користувач "user1" додає канал #2:
→ total_users: 11  (не змінюється, вже є канали)
→ total_channels: 17  ✅ (+1)
→ total_posts: 500
```

### Приклад 2: Користувач видаляє канали
```
Користувач "user1" має 2 канали

Видаляє канал #1:
→ total_users: 11  (залишається, є ще канал #2)
→ total_channels: 16  ✅ (-1)

Видаляє канал #2:
→ total_users: 10  ✅ (-1, більше немає каналів)
→ total_channels: 15  ✅ (-1)
```

### Приклад 3: Публікація постів
```
Бот публікує пост:
→ total_posts: 501  ✅ (+1)

Бот публікує ще 10 постів:
→ total_posts: 511  ✅ (+10)

Користувач видаляє 3 опубліковані пости:
→ total_posts: 508  ✅ (-3)

Користувач видаляє 5 scheduled постів:
→ total_posts: 508  (не змінюється, вони не були опубліковані)
```

---

## 🧪 ТЕСТУВАННЯ

### SQL запити для перевірки:

```sql
-- Переглянути статистику всіх ботів
SELECT 
  tb.bot_name,
  tb.bot_username,
  bgs.total_users,
  bgs.total_channels,
  bgs.total_posts
FROM bot_global_stats bgs
JOIN telegram_bots tb ON tb.id = bgs.bot_id;

-- Порівняти з реальними даними
SELECT 
  bs.bot_id,
  COUNT(DISTINCT bs.user_id) as real_users,
  COUNT(bs.id) as real_channels
FROM bot_services bs
GROUP BY bs.bot_id;

-- Перерахувати статистику (якщо щось не синхронізовано)
SELECT * FROM recalculate_bot_global_stats();
```

---

## 🔄 МІГРАЦІЇ

Створено 2 міграції:

1. **`20251230000000_add_bot_global_stats.sql`**
   - Таблиця `bot_global_stats`
   - Тригери для каналів
   - Функція ініціалізації
   - Функція перерахунку

2. **`20251230000001_add_post_count_triggers.sql`**
   - Тригери для постів
   - Функції інкременту/декременту

---

## ✅ ПЕРЕВАГИ

1. **Реальна статистика** - показує скільки людей реально користується ботом
2. **Автоматичне оновлення** - не потрібно вручну рахувати
3. **Точність** - враховує видалення каналів та користувачів
4. **Продуктивність** - одна таблиця замість складних агрегацій
5. **Наочність** - користувачі бачать популярність бота

---

## 📝 ФАЙЛИ ЩО БУЛИ ЗМІНЕНІ

1. ✅ `promebot/supabase/migrations/20251230000000_add_bot_global_stats.sql` (НОВА)
2. ✅ `promebot/supabase/migrations/20251230000001_add_post_count_triggers.sql` (НОВА)
3. ✅ `promebot/src/components/BotSelector.tsx` (ЗМІНЕНА)

---

## 🎯 РЕЗУЛЬТАТ

Тепер на сторінці вибору бота користувачі бачать:

```
┌────────────────────────────────┐
│ 🤖 Бот Максим                  │
│ @maximepromo_bot               │
│                                │
│ 📊 Загальна статистика         │
│ ┌──────┬──────────┬──────────┐ │
│ │  15  │    23    │   1542   │ │
│ │Кор-в │ Каналів  │  Постів  │ │
│ └──────┴──────────┴──────────┘ │
│                                │
│ Uptime: 85.2%                  │
│ Навантаження: 🟢 85.2%         │
└────────────────────────────────┘
```

Статистика оновлюється автоматично при будь-яких діях користувачів!
